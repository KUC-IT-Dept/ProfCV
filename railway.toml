[build]
builder = "NIXPACKS"
watchPatterns = ["server/**"]
buildCommand = "if [ -d server ]; then cd server; fi; npm ci"

[deploy]
startCommand = "if [ -d server ]; then cd server; fi; npm start"
restartPolicyType = "ON_FAILURE"
